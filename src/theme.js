import React from "react"
import { createMuiTheme } from "@material-ui/core/styles"

const theme = createMuiTheme({
  palette: {
    primary: { main: "#202124" },
    secondary: { main: "#474747" },
  },
})

export default theme
